/** Types the project registry generator for focused contract tests. */

export function renderProjectRegistry(): Promise<string>;
export function syncProjectRegistry(checkOnly?: boolean): Promise<boolean>;
